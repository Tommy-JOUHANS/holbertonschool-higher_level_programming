-- Create a table named `first_table` with the following columns:
-- - `id`: an integer column to store the unique identifier for each record.
-- - `name`: a variable character column with a maximum length of 255 characters to store the name associated with each record.
CREATE TABLE first_table (
    id INT,
    name VARCHAR(255)
);